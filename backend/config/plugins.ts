export default ({}) => ({
    "users-permissions": {
        config: {
            register: {
                allowedFields: ["firstname", "lastname", "gender", "birthdate"]
            }
        }
    }
});
                